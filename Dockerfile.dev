FROM node:20-slim
WORKDIR /app

# Install deps (including devDeps — needed for tsx, tailwind, types)
COPY package*.json ./
RUN npm ci

# Source is bind-mounted at runtime, not baked in
CMD ["npm", "run", "dev"]
